//ini cara imoport per function / method (sayPackage())
//import com.yogiflogic.belajarPackage.util.sayPackage

// Ini cara import seluruh isi package dengan menggunakan tanda bintang (*)
import com.yogiflogic.belajarPackage.util.*

fun main() {
    sayPackage(" Ini bagain Yang Di Function MAIN Kotlin Package & Import")
    println()
    sayImport("Ini bagian di Main Kotlin Import")
}